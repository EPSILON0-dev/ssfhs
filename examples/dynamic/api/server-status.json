{
  "cpuType": "<ssfhs-dyn>echo `cat /proc/cpuinfo | grep "model name" | head -1 | cut -d':' -f2 | xargs` | head -c -1</ssfhs-dyn>s",
  "cpuUsage": "Secret CPU usage data is not available :3",
  "memoryUsage": "<ssfhs-dyn>
  total=`cat /proc/meminfo | grep MemTotal | cut -d':' -f2 | xargs | cut -d' ' -f1`
  available=`cat /proc/meminfo | grep MemAvailable | cut -d':' -f2 | xargs | cut -d' ' -f1`
  printf "Server RAM: "
  printf "`echo "scale=1; u = ($total - $available); u / 1024 / 1024" | bc`/"
  printf "`echo "scale=1; t = ($total); t / 1024 / 1024" | bc`GB"
  </ssfhs-dyn>",
  "requestId": "<ssfhs-dyn>echo $REQUEST_ID | head -c -1</ssfhs-dyn>",
  "uptime": "<ssfhs-dyn>uptime -p | head -c -1</ssfhs-dyn>",
  "serverTime": "<ssfhs-dyn>date +'%H:%M:%S' | head -c -1</ssfhs-dyn>"
}
